// Intentionally empty — Better Auth handles all credential and session logic.
// If you need to extend auth behaviour (e.g. post-registration hooks),
// use Better Auth's plugin system in src/lib/auth.ts.

export {}
