// Global type augmentations for Fastify.
// Better Auth session user is attached to FastifyRequest via auth.plugin.ts.
// Import BetterAuthUser from there rather than duplicating the type here.

export {}
